# Profood Livreur App — environment variables
# Copy this file to .env.local for local development.

# App key sent in the POST /signin request body.
# This gates role access on the backend.
#
# BACKEND GAP: Until a LIVREUR role and its own app key are added to the
# Laravel API, use the manager app key here and create driver accounts
# with the MANAGER role.  See TODO_BACKEND_GAPS.md.
VITE_PROFOOD_APP_KEY=your_profood_app_key_here

# Optional: override the API base URL for local development.
# Defaults to http://localhost:8000/api/ in dev and https://api.profood-app.com/api/ in prod.
# VITE_API_BASE_URL=http://localhost:8000/api/
